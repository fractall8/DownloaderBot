services:
  redis:
    image: redis:alpine
    container_name: downloaderbot-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    # Memory limit
    deploy:
      resources:
        limits:
          memory: 256M

volumes:
  redis_data: